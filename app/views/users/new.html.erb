<button id="owner-button">Register As a Pet Owner</button>
<button>Register As an Organization</button>

<%= render "form" %>
<% if @errors %>
<div class="errors">
    <% @errors.each do |error| %>
    <li><%= error %></li>
    <% end %>
  <% end %>
</div>
