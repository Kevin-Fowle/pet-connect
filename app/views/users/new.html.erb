<div class="center-content">
  <div class="reg-buttons">
    <p>
      Register as a...
    </p>
    <button id="owner-button" class="waves-effect waves-light btn">Pet Owner</button>
    <p>
      or an
    </p>
    <button id="organization-button" class="waves-effect waves-light btn" onclick="location.href='http://localhost:3000/organizations/new';">Organization</button>
  </div>

  <%= render "form" %>
    <% if @errors %>
      <div class="errors">
      <% @errors.each do |error| %>
      <p><%= error %></p>
      <% end %>
    <% end %>
  </div>
</div>
